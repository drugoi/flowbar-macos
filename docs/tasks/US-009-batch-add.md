# US-009 Batch Add URLs

As a power user, I want to add multiple YouTube URLs at once so I can build my library quickly.

## Acceptance Criteria
- Add flow accepts multiple URLs pasted at once.
- Invalid URLs are rejected with clear per-item errors.
- Valid items are added to the library in the order provided.
- Batch add does not block the UI while downloads start.
